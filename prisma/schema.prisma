// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://kunafa_chat_user:9xQpI4WDwkKliW7f7C8JkJ8PJGuVteuX@dpg-d7mvvaj7uimc73b5om10-a.oregon-postgres.render.com/kunafa_chat"
}

model Message {
  id        Int     @id @default(autoincrement())
  sender    String  // "manil" or "ines"
  text      String
  timestamp DateTime @default(now())
  
  @@index([timestamp])
}

model Session {
  id        Int     @id @default(autoincrement())
  user      String  // "manil" or "ines"
  token     String  @unique
  createdAt DateTime @default(now())
  expiresAt DateTime
}
